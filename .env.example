# Banter Poker shares the OrbitStack Supabase project and owns the `banterpoker` schema.
# Only the public URL and publishable (anon) key are needed: every privileged operation
# runs inside Postgres security-definer functions that verify a per-device secret.
NEXT_PUBLIC_SUPABASE_URL=https://pfagkivkytrvbkhsulvo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
NEXT_PUBLIC_SITE_URL=http://localhost:3010
